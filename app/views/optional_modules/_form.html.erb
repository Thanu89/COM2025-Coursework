<%= form_with(model: optional_module, local: true) do |form| %>
  <% if optional_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(optional_module.errors.count, "error") %> prohibited this optional_module from being saved:</h2>

      <ul>
      <% optional_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>
  <% current_account.id %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
